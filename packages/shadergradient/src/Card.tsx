import * as React from "react";
import { Button } from "./Button";

export const Card = () => {
  return (
    <div
      style={{
        background: "pink",
        padding: 20,
      }}
    >
      <Button />
      CAARD
    </div>
  );
};
