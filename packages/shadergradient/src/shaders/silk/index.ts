// Silk shares one vertex/fragment source across all three geometries; the
// sphere variant only adds a #define so uFrequency / uAmplitude twist it like
// the defaults sphere does.
export * as plane from './plane/index'
export * as sphere from './sphere/index'
export * as waterPlane from './waterPlane/index'
